# MCP transport: stdio or httpStream
MCP_TRANSPORT=stdio

# HTTP Streamable transport port when MCP_TRANSPORT=httpStream or --http-stream is used
PORT=8080

# Optional auth placeholder for future HTTP deployments
MCP_API_KEY=

# Optional cache TTL for future GitHub/official-doc KB refresh features
PML_KB_CACHE_TTL_SECONDS=86400
